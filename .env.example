# 环境变量配置示例
# 复制此文件为 .env 并填入真实值（.env 已在 .gitignore 中，不会被提交到 Git）

# 应用端口
PORT=3001

# Spring Profile (dev, prod)
SPRING_PROFILES_ACTIVE=dev

# 数据库配置
DATABASE_URL=jdbc:mysql://localhost:3306/volcano_blog?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DATABASE_USERNAME=root
DATABASE_PASSWORD=your_database_password

# JWT 配置
# 警告：生产环境必须使用强密钥！
# 使用以下命令生成密钥: openssl rand -base64 32
# 或使用 PowerShell: [Convert]::ToBase64String((New-Object byte[] 32))
JWT_SECRET=your_base64_encoded_secret_at_least_32_characters_long
JWT_EXPIRES_IN_MS=604800000

# CORS 配置（允许的前端地址，多个地址用逗号分隔）
CORS_ORIGIN=http://localhost:5173,http://localhost:5174
